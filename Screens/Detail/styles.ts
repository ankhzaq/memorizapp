import { StyleSheet } from 'react-native';

const styles =
  StyleSheet.create({
    input: {
      marginBottom: 10,
    },
    layout: {
      paddingHorizontal: 10,
    },
    firstSection: {
      marginRight: 10,
    },
    sections: {
      alignItems: 'center',
      flexDirection: 'row',
      justifyContent: 'center',
      marginBottom: 15,
      width: '100%',
    }
  });
export default styles;
